/**
 * Form type constants for different form configurations
 * Used to identify which type of form is being displayed
 */
export const forms = {
    USERS: 'USERS',
    CHILDREN: 'CHILDREN',
    BIND: 'BIND'
}
